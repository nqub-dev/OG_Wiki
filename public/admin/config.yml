# GENERATED by scripts/gen-cms-config.mjs — do not edit by hand.
# Runs automatically as part of `npm run build`.
# To change it: edit wiki.config.ts or add a folder under src/content/docs/,
# then `npm run gen:cms`.

backend:
  name: github
  repo: nqub-dev/OG_Wiki
  branch: main
  # No base_url on purpose. Sveltia CMS falls back to Netlify's built-in OAuth
  # client when authentication isn't configured explicitly, which is what we
  # want on Netlify — no Cloudflare Worker, no client secret in the repo.
  #
  # If this wiki moves off Netlify, deploy sveltia-cms-auth as a Cloudflare
  # Worker and point base_url at it:
  #   base_url: https://sveltia-cms-auth.<subdomain>.workers.dev

# Editors drop images here; Markdown references them via public_folder.
media_folder: public/uploads
public_folder: /uploads

locale: en

collections:
  - name: engineering
    label: "Engineering"
    label_singular: page
    folder: src/content/docs/engineering
    create: true
    delete: true
    extension: md
    format: yaml-frontmatter
    slug: '{{slug}}'
    summary: '{{title}}'
    sortable_fields: [order, title, updated]
    # Mirrors the Zod schema in src/content.config.ts. If these drift apart the
    # build fails loudly rather than publishing a broken page.
    fields:
      - { name: title, label: Title, widget: string }
      - { name: description, label: Description, widget: text, required: false }
      - { name: icon, label: Icon (emoji), widget: string, default: '📄', required: false }
      # Pinned to this folder so frontmatter can't disagree with location.
      - { name: section, label: Section, widget: hidden, default: "Engineering" }
      - { name: order, label: Sort order, widget: number, default: 999, value_type: int }
      - { name: tags, label: Tags, widget: list, required: false }
      - name: status
        label: Status
        widget: select
        default: draft
        options: [draft, review, stable, deprecated]
      - {
          name: updated,
          label: Last updated,
          widget: datetime,
          format: 'YYYY-MM-DD',
          date_format: 'YYYY-MM-DD',
          time_format: false,
          required: false,
        }
      - { name: owner, label: Owner, widget: string, required: false }
      - { name: draft, label: Hide from the published site, widget: boolean, default: false }
      - { name: featured, label: Feature on the home page, widget: boolean, default: false }
      - { name: body, label: Page content, widget: markdown }

  - name: handbook
    label: "Handbook"
    label_singular: page
    folder: src/content/docs/handbook
    create: true
    delete: true
    extension: md
    format: yaml-frontmatter
    slug: '{{slug}}'
    summary: '{{title}}'
    sortable_fields: [order, title, updated]
    # Mirrors the Zod schema in src/content.config.ts. If these drift apart the
    # build fails loudly rather than publishing a broken page.
    fields:
      - { name: title, label: Title, widget: string }
      - { name: description, label: Description, widget: text, required: false }
      - { name: icon, label: Icon (emoji), widget: string, default: '📄', required: false }
      # Pinned to this folder so frontmatter can't disagree with location.
      - { name: section, label: Section, widget: hidden, default: "Handbook" }
      - { name: order, label: Sort order, widget: number, default: 999, value_type: int }
      - { name: tags, label: Tags, widget: list, required: false }
      - name: status
        label: Status
        widget: select
        default: draft
        options: [draft, review, stable, deprecated]
      - {
          name: updated,
          label: Last updated,
          widget: datetime,
          format: 'YYYY-MM-DD',
          date_format: 'YYYY-MM-DD',
          time_format: false,
          required: false,
        }
      - { name: owner, label: Owner, widget: string, required: false }
      - { name: draft, label: Hide from the published site, widget: boolean, default: false }
      - { name: featured, label: Feature on the home page, widget: boolean, default: false }
      - { name: body, label: Page content, widget: markdown }

  - name: operations
    label: "Operations"
    label_singular: page
    folder: src/content/docs/operations
    create: true
    delete: true
    extension: md
    format: yaml-frontmatter
    slug: '{{slug}}'
    summary: '{{title}}'
    sortable_fields: [order, title, updated]
    # Mirrors the Zod schema in src/content.config.ts. If these drift apart the
    # build fails loudly rather than publishing a broken page.
    fields:
      - { name: title, label: Title, widget: string }
      - { name: description, label: Description, widget: text, required: false }
      - { name: icon, label: Icon (emoji), widget: string, default: '📄', required: false }
      # Pinned to this folder so frontmatter can't disagree with location.
      - { name: section, label: Section, widget: hidden, default: "Operations" }
      - { name: order, label: Sort order, widget: number, default: 999, value_type: int }
      - { name: tags, label: Tags, widget: list, required: false }
      - name: status
        label: Status
        widget: select
        default: draft
        options: [draft, review, stable, deprecated]
      - {
          name: updated,
          label: Last updated,
          widget: datetime,
          format: 'YYYY-MM-DD',
          date_format: 'YYYY-MM-DD',
          time_format: false,
          required: false,
        }
      - { name: owner, label: Owner, widget: string, required: false }
      - { name: draft, label: Hide from the published site, widget: boolean, default: false }
      - { name: featured, label: Feature on the home page, widget: boolean, default: false }
      - { name: body, label: Page content, widget: markdown }

  - name: reference
    label: "Reference"
    label_singular: page
    folder: src/content/docs/reference
    create: true
    delete: true
    extension: md
    format: yaml-frontmatter
    slug: '{{slug}}'
    summary: '{{title}}'
    sortable_fields: [order, title, updated]
    # Mirrors the Zod schema in src/content.config.ts. If these drift apart the
    # build fails loudly rather than publishing a broken page.
    fields:
      - { name: title, label: Title, widget: string }
      - { name: description, label: Description, widget: text, required: false }
      - { name: icon, label: Icon (emoji), widget: string, default: '📄', required: false }
      # Pinned to this folder so frontmatter can't disagree with location.
      - { name: section, label: Section, widget: hidden, default: "Reference" }
      - { name: order, label: Sort order, widget: number, default: 999, value_type: int }
      - { name: tags, label: Tags, widget: list, required: false }
      - name: status
        label: Status
        widget: select
        default: draft
        options: [draft, review, stable, deprecated]
      - {
          name: updated,
          label: Last updated,
          widget: datetime,
          format: 'YYYY-MM-DD',
          date_format: 'YYYY-MM-DD',
          time_format: false,
          required: false,
        }
      - { name: owner, label: Owner, widget: string, required: false }
      - { name: draft, label: Hide from the published site, widget: boolean, default: false }
      - { name: featured, label: Feature on the home page, widget: boolean, default: false }
      - { name: body, label: Page content, widget: markdown }

  - name: start-here
    label: "Start Here"
    label_singular: page
    folder: src/content/docs/start-here
    create: true
    delete: true
    extension: md
    format: yaml-frontmatter
    slug: '{{slug}}'
    summary: '{{title}}'
    sortable_fields: [order, title, updated]
    # Mirrors the Zod schema in src/content.config.ts. If these drift apart the
    # build fails loudly rather than publishing a broken page.
    fields:
      - { name: title, label: Title, widget: string }
      - { name: description, label: Description, widget: text, required: false }
      - { name: icon, label: Icon (emoji), widget: string, default: '📄', required: false }
      # Pinned to this folder so frontmatter can't disagree with location.
      - { name: section, label: Section, widget: hidden, default: "Start Here" }
      - { name: order, label: Sort order, widget: number, default: 999, value_type: int }
      - { name: tags, label: Tags, widget: list, required: false }
      - name: status
        label: Status
        widget: select
        default: draft
        options: [draft, review, stable, deprecated]
      - {
          name: updated,
          label: Last updated,
          widget: datetime,
          format: 'YYYY-MM-DD',
          date_format: 'YYYY-MM-DD',
          time_format: false,
          required: false,
        }
      - { name: owner, label: Owner, widget: string, required: false }
      - { name: draft, label: Hide from the published site, widget: boolean, default: false }
      - { name: featured, label: Feature on the home page, widget: boolean, default: false }
      - { name: body, label: Page content, widget: markdown }
